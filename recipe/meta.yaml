{% set version = "0.22.2" %}
{% set python_min = "3.8" %}

package:
  name: movingpandas
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/m/movingpandas/movingpandas-{{ version }}.tar.gz
  sha256: 50b36aad4b85804ebce768b16f560e7011ce7a5129d1ba2e89a18ed64d03fdd0

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - geopandas-base
    - geopy
    - matplotlib-base
    - pyproj

test:
  requires:
    - python {{ python_min }}
  imports:
    - movingpandas

about:
  home: https://github.com/movingpandas/movingpandas
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: 'MovingPandas provides trajectory data structures and functions for handling movement data'

extra:
  recipe-maintainers:
    - raybellwaves
    - anitagraser
    - jorisvandenbossche
